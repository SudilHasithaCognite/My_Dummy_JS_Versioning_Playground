{
  "name": "my-dummy-js-versioning-playground",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream --scope=@my-dummy-js-versioning-playground/{header,footer} && lerna run build --stream --scope=@my-dummy-js-versioning-playground/remixapp",
    "test": "lerna run test",
    "docs:bundle": "echo 'no docs yet'"
  },
  "devDependencies": {
    "lerna": "^7.4.2"
  }
}
