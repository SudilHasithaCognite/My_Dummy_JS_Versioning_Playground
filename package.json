{
  "name": "my-dummy-js-versioning-playground",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install": "yarn install --immutable",
    "build": "lerna run build --stream",
    "test": "lerna run test",
    "docs:bundle": "echo 'no docs yet'"
  },
  "devDependencies": {
    "lerna": "^7.1.4"
  }
}